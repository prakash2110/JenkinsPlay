@Library('jenkins-library@master')_
@Library('pipeline-library-demo@master')_
pipeline {
    agent any

    stages {
        stage("Env Variables") {
            steps {
                sh "printenv"
		sayHello1 'Shree'
		sayHello1 'Prakash'
            }
        }
    }
}
