@Library('pipeline-library-demo')_
@Library('pipeline-library')_
pipeline {
    agent any

    stages {
        stage("Env Variables") {
            steps {
                sh "printenv"
		sayHello 'Dave'
		Hello1 'Prakash'
            }
        }
    }
}
