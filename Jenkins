@Library('jenkins-library@master') _
pipeline {
    agent any

    stages {
        stage("Env Variables") {
            steps {
                sh "printenv"
		sayHello1 'Prakash'
            }
        }
    }
}
