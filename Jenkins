@Library('jenkins-library@master') l1
@Library('pipeline-library-demo@master') l2
pipeline {
    agent any

    stages {
        stage("Env Variables") {
            steps {
                sh "printenv"
		sayHello1 'Shree'
		sayHello1 'Prakash'
            }
        }
    }
}
