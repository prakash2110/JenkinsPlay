@Library('pipeline-library-demo')_
@Library('pipeline-library')
pipeline {
    agent any

    stages {
        stage("Env Variables") {
            steps {
                sh "printenv"
		sayHello 'Dave'
		Hello1 'Prakash'
            }
        }
    }
}
