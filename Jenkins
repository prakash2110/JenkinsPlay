@Library('jenkins-library@master')_ l1
@Library('pipeline-library-demo@master')_ l2
pipeline {
    agent any

    stages {
        stage("Env Variables") {
            steps {
                sh "printenv"
		sayHello1 'Shree'
		sayHello1 'Prakash'
            }
        }
    }
}
