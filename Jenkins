@Library('jenkins-library@master') l1
@Library('pipeline-library-demo@master') l2
pipeline {
    agent any

    stages {
        stage("Env Variables") {
            steps {
                sh "printenv"
		hello1 'Shree'
		hello1 'Prakash'
            }
        }
    }
}
