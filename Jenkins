@Library('pipeline-library-demo')_
@Library('pipeline-library')
import Hello1
pipeline {
    agent any

    stages {
        stage("Env Variables") {
            steps {
                sh "printenv"
		sayHello 'Dave'
		sayHello1 'Prakash'
            }
        }
    }
}
