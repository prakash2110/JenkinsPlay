@Library('pipeline-library-demo')_
pipeline {
    agent any

    stages {
        stage("Env Variables") {
            steps {
                sh "printenv"
		sayHello 'Dave'
            }
        }
    }
}
